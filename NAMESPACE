## NAMESPACE for MultiModalGraphics

## ---- Exports ----
export(ClearScatterplot)
export(ClearScatterplot_MAE)
export(ClearScatterplot_table)
export(InformativeHeatmap)
export(InformativeHeatmapFromMAE)
export(MultifeatureGrid)
export(createPlot)
export(getHeatmapObject)
export(get_clear_scatterplot_df)
export(get_informative_heatmap_df)
export(get_multifeature_grid_df)
export(plot_heatmap)
export(updateLayerFun)

## ---- Exported S4 Methods ----
exportMethods(ClearScatterplot)
exportMethods(InformativeHeatmap)
exportMethods(createPlot)
exportMethods(getHeatmapObject)
exportMethods(plot_heatmap)
exportMethods(show)
exportMethods(updateLayerFun)

## ---- Imports from base R / Recommended Packages ----
import(methods)

importFrom(stats, model.matrix, quantile, as.formula)

## ---- Imports from Bioconductor / CRAN Packages ----
importFrom(ComplexHeatmap, Heatmap, restore_matrix)
importFrom(MultiAssayExperiment, assay, colData, experiments, sampleMap)
importFrom(SummarizedExperiment, assay)           # in case some methods call SummarizedExperiment::assay
importFrom(BiocParallel, bplapply, bpworkers)
importFrom(limma, lmFit, eBayes, topTable, voom)
importFrom(matrixStats, rowVars)
importFrom(RColorBrewer, brewer.pal)
importFrom(circlize, colorRamp2)                  # used in InformativeHeatmap and MultiModalPlot
importFrom(patchwork, `+`)                        # for combining ggplots in MultiModalPlot
importFrom(scales, comma_format)                  # for MultifeatureGrid color scale labels
importFrom(grid, grid.points, unit, gpar)         # used in InformativeHeatmap layer_fun
importFrom(grDevices, colorRampPalette)           # used in MultifeatureGrid

## ---- Imports from dplyr / magrittr / ggplot2 ----
importFrom(magrittr, "%>%")
importFrom(dplyr, group_by, summarize, tally)

importFrom(ggplot2,
           ggplot,
           aes,
           aes_string,
           geom_jitter,
           geom_point,
           geom_text,
           geom_tile,
           scale_color_gradient,
           scale_color_manual,
           scale_fill_gradientn,
           scale_fill_hue,
           scale_size,
           labs,
           facet_grid,
           theme,
           theme_bw,
           element_text,
           element_rect,
           element_line,
           element_blank)

## ---- Imports from Matrix or Other Utility Packages (if used) ----
# (None additional for these three classes; remove if unused)

## ---- End of NAMESPACE ----

